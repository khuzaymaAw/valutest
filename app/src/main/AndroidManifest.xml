<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.valu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".coin_sending_screen"></activity>
        <activity android:name=".drag_view" />
        <activity android:name=".javaToKotlin" />
        <activity android:name=".central_page" />
        <activity android:name=".video_explanation_page" />
        <activity android:name=".rules_page_four" />
        <activity android:name=".rule_page_three" />
        <activity android:name=".rules_page_two" />
        <activity android:name=".rules_page_one" />
        <activity android:name=".junction_screen" />
        <activity android:name=".lottie_screen" />
        <activity android:name=".coin_screen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>